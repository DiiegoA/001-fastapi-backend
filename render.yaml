services:
  - type: web
    name: fastapi-backend
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: DATABASE_URL
        value: "postgresql+psycopg2://todo_user:Lauracamila95*@dpg-abc12345abcdef.render.com:5432/todos_db
"
